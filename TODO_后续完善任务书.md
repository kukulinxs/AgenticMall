# AgenticMall Demo 后续完善任务书 / TODO

> 目标：在保持“轻量商品运维 Demo”边界的前提下，逐步补齐权限、接口契约、后台体验和工程质量。
>
> 当前基线：`release/v0.1.1`，已具备 FastAPI 商品网关、5 个 Skill、轻量 Vue 商品后台、uv 环境和基础业务测试。

## 一、范围边界

### 本阶段继续保留

- 商品列表、筛选、新增、编辑、上下架、删除。
- TeleAgent -> MCP 风格网关 -> 商品业务函数 -> Mock 商品后台的调用链。
- 单体 FastAPI + 轻量 Vue 3/Vite/Element Plus。
- Demo 可使用内存数据；只有在确实需要重启保留数据时再引入 SQLite。

### 暂不引入

- 订单、会员、促销、财务、营销等完整商城模块。
- Redis、Elasticsearch、消息队列和微服务拆分。
- 完整企业级用户中心、复杂 RBAC、OAuth/OIDC。
- SKU、分类树、图片上传和库存流水。

## 二、认证方案决策（需要先定版）

### 2.1 当前问题

当前 `/mcp/invoke` 校验 `X-Demo-Token`，但 `/api/shop/goods/*` 后台接口没有鉴权。这样前端可以绕过网关直接新增、修改、上下架和删除商品，权限模型不一致。

### 2.2 对“让星辰智能体登录账号密码”的评估

这个思路在技术上可行，但不适合作为当前 Demo 的默认方案：

- 账号密码会进入智能体对话或工具调用链，存在泄露和日志暴露风险。
- 智能体是否能安全保存登录态、刷新 Token、跨轮次复用 Token，取决于星辰平台能力，不能假定一定支持。
- 如果 Skill 每次都让模型重新组织账号密码，会增加误调用和越权风险。
- 登录页、验证码、过期、刷新、退出等流程会显著扩大 Demo 范围。

### 2.3 推荐方案：分两步实施

#### MVP 推荐：预配置演示 Token（优先落地）

- 服务端保留两个演示身份：`admin`、`operator`。
- 星辰 Skill 通过平台安全变量或密钥配置携带 Token，不在对话中出现密码。
- `/mcp/invoke` 和 `/api/shop/goods/*` 使用同一套服务端鉴权函数。
- 前端后台也携带管理员 Token，或明确标注为“仅本机可信演示入口”。

建议请求头统一为：

```text
Authorization: Bearer <demo-token>
```

为兼容当前 Skill，可暂时同时支持 `X-Demo-Token`，后续统一迁移。

#### 可选增强：轻量登录换取短期 Token

仅当星辰平台确认支持“安全变量 + 会话级 Token 保存 + 自定义 Authorization 请求头”后再做：

1. `POST /api/auth/login` 接收演示账号密码。
2. 服务端校验硬编码 Demo 账号，返回内存中的短期 opaque session token。
3. 后续请求只携带 Token，不再发送密码。
4. Token 过期、退出和服务重启失效均写入文档。

该方案仍然只是 Demo 登录模拟，不应伪装成生产级认证。

### 2.4 本任务书的默认决策

在未确认星辰平台支持安全登录态前，采用“预配置演示 Token”方案，不让智能体从用户对话中收集或展示账号密码。

## 三、P0：安全与基础契约（下一阶段必须完成）

- [ ] **P0-01 统一后台接口鉴权**
  - 为 `/api/shop/goods/*` 增加与网关一致的服务端鉴权依赖。
  - 明确查询是否允许 `operator`，写操作是否只允许 `admin`。
  - 验收：运营 Token 不能直接调用新增、修改、删除；伪造请求体 `role_id` 不能越权。

- [ ] **P0-02 统一 Token 传递方式**
  - 选定 `Authorization: Bearer` 或继续使用 `X-Demo-Token`，并在 API、Skill、前端、README 中统一。
  - Token 只能来自平台安全配置/环境变量，不出现在对话和普通日志。

- [ ] **P0-03 统一服务地址配置**
  - 解决 `8080`、`8081`、`8082` 多处硬编码问题。
  - 后端、Vite 代理、Skill 模板、README 使用同一个配置说明。
  - 验收：修改一个环境变量即可切换后端端口。

- [ ] **P0-04 增加统一异常处理**
  - 未处理异常对外返回安全的 HTTP 500 和固定结构。
  - 日志记录异常详情，响应不得泄露堆栈、Token 或本机路径。

- [ ] **P0-05 完善结构化日志**
  - 至少记录时间、session_id、声明角色、解析角色、skill、参数摘要、鉴权结论、响应码。
  - 参数需截断/脱敏，禁止记录认证信息。

- [ ] **P0-06 补充 HTTP 层测试**
  - 覆盖请求头、HTTP 422、CORS、后台接口鉴权和 `/mcp/invoke`。
  - 保留现有纯业务函数测试。

## 四、P1：商品领域与接口增强

- [ ] **P1-01 明确查询不存在语义**
  - 列表无结果返回 `code=0,data=[]`。
  - 按 `goods_id` 查询不存在返回 `2001`，或在文档中明确继续采用空数组。
  - Skill 和前端提示保持一致。

- [ ] **P1-02 增加 `updated_at`**
  - 修改价格、库存、部门、描述或状态时更新该字段。
  - 前端“更新时间”不得继续展示 `create_time`。

- [ ] **P1-03 增加轻量分页与排序**
  - 查询支持 `page`、`page_size`、排序字段和方向。
  - 返回 `{items,total,page,page_size}`。
  - 只在数据量或演示需求确实需要时实施。

- [ ] **P1-04 抽象轻量 Repository 边界**
  - 将商品业务函数与 `goods_db` 读写解耦。
  - 先提供内存实现；未来需要持久化时再增加 SQLite 实现，不改 Skill 契约。

- [ ] **P1-05 增加重置能力**
  - 增加仅供 Demo 使用的 reset 机制或明确的重启重置命令。
  - 不允许普通 Agent 在无确认的情况下重置数据。

- [ ] **P1-06 删除操作幂等与确认**
  - Agent 删除前必须向用户二次确认。
  - 删除不存在仍稳定返回 `2001`。
  - Skill 不展示原始异常。

## 五、P1：轻量后台完善

- [ ] **P1-07 前端接入统一认证**
  - 管理端通过环境变量或本地 Demo 配置携带管理员 Token。
  - 页面显示当前演示身份，不实现完整登录系统。

- [ ] **P1-08 统一前端 API 配置**
  - 使用 `.env.development` / `.env.local` 配置后端地址和 Demo Token。
  - `vite.config.js` 不再固定写死 `8082`。

- [ ] **P1-09 修正统计口径**
  - “当前商品、已上架、库存总量”明确是筛选结果还是全局统计。
  - MVP 可先将文案改为“当前筛选结果”。

- [ ] **P1-10 增加分页和更清晰的空状态**
  - 只有商品数量明显增长后再启用分页组件。
  - 区分加载失败、无筛选结果和服务未启动。

- [ ] **P1-11 增加最小前端回归检查**
  - 保持 `pnpm run build`。
  - 暂不引入大型 E2E 测试框架，除非交互复杂度明显上升。

## 六、P2：Agent / Skill 体验

- [ ] **P2-01 Skill 配置参数化**
  - 网关地址、Token 变量名、环境区分统一管理。
  - 不在五份 Skill 中重复维护易变地址。

- [ ] **P2-02 补齐错误码中文反馈**
  - 统一 `1001/1002/1003/2001~2005` 的反馈语句。
  - Agent 不向用户展示内部路径、原始 msg 或 Token。

- [ ] **P2-03 明确会话与 Token 生命周期**
  - 记录 session_id 的用途。
  - 若启用登录模拟，记录 Token 过期、服务重启失效和刷新策略。

- [ ] **P2-04 增加典型对话验收脚本**
  - 新增缺参追问。
  - 运营越权。
  - 管理员新增/修改/上下架/删除。
  - 删除确认。
  - 业务错误中文转译。

## 七、P2：工程质量与交付

- [ ] **P2-05 修复前端构建在受限环境下的可重复性**
  - 保留 `pnpm-lock.yaml`。
  - 文档记录 pnpm 版本和权限/缓存要求。

- [ ] **P2-06 增加 CI 最小流水线**
  - Python 编译和 unittest。
  - 前端依赖安装和 build。
  - 检查 `.gitignore` 未遗漏依赖与构建产物。

- [ ] **P2-07 更新 README 和接口文档**
  - 统一启动端口、认证方式、Skill 导入方式、后台入口和重置规则。
  - 给出管理员/运营各一条可复制请求示例。

- [ ] **P2-08 版本化接口契约**
  - 在保持 Demo 简洁的前提下，为网关和商品接口增加版本说明，例如 `/api/v1`。
  - 仅在确实出现兼容性需求时实施。

## 八、建议开发顺序与里程碑

### 里程碑 M1：可信 Demo（优先）

完成 `P0-01` 至 `P0-06`，验收标准：

- 网关和后台接口使用同一套身份权限。
- 运营 Token 无法执行管理员写操作。
- 所有异常响应和日志满足安全约束。
- HTTP 层和业务层测试均通过。

### 里程碑 M2：可持续商品后台

完成 `P1-01` 至 `P1-06`，验收标准：

- 查询、修改、状态变更、删除语义稳定。
- 前端时间字段和统计口径正确。
- 数据量增加后仍可分页操作。
- 需要持久化时只替换 Repository，不改 Agent 契约。

### 里程碑 M3：Agent 协同体验

完成 `P1-07` 至 `P2-04`，验收标准：

- 后台和 Agent 操作同一份数据。
- Token 不进入对话文本。
- 删除等副作用操作有明确确认。
- 五个 Skill 的典型对话脚本全部通过。

### 里程碑 M4：可交付工程

完成 `P2-05` 至 `P2-08`，验收标准：

- 新机器按 README 可复现启动。
- CI 可自动验证后端和前端。
- 当前分支可以安全合并或发布。

## 九、当前推荐决策

下一次开发不要先做“智能体账号密码登录”。先完成：

1. 后台接口接入服务端统一鉴权；
2. 在星辰平台用安全变量配置管理员/运营 Token；
3. 统一 `Authorization`/`X-Demo-Token` 方案和服务地址；
4. 增加 HTTP 层测试；
5. 只有在确认星辰支持安全保存登录态后，再评估轻量登录换 Token。

